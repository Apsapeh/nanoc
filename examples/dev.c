#include <stdio.h>
#define Aboba 10

#define multiline(a, b) \
    a + b

#define M2 (a, b) a + b

int a; // Single line comment

const char* str = "Hello  oeu";

void print(const char * a) {
    __asm__ {
        read b char

    }}

int func            (int /**/ a) {
    return a;
}

int main() {
    return func(Aboba);
}
